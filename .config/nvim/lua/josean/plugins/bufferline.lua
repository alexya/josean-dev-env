return {
  "akinsho/bufferline.nvim",
  dependencies = { "nvim-tree/nvim-web-devicons" },
  version = "*",
  -- tag = "v4.5.2",  -- to resolve a runtime issue on Windows OS, and need to comment out above line version = "*"
  opts = {
    options = {
      mode = "tabs",
    },
  },
}
